/*Create customers database with customers collections added*/

const database = 'customers';
const collection = 'customer';

// Create a new database.
use(database);

// Create a new collection.
db.createCollection(collection);

