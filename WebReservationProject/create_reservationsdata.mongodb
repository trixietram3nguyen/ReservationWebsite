/*Create reservations database with tables collections added*/

const database = 'reservations';
const collection = 'tables';

// Create a new database.
use(database);

// Create a new collection.
db.createCollection(collection);

