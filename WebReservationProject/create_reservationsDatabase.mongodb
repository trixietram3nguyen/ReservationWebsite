/*Create reservations database for a restaurant*/

//drop the database
//create for purpose of testing
//use('reservations')
//db.dropDatabase()

const database = 'reservations';
const customer_collections = 'customers';
const table_collections = 'tables';

// Create a new database.
use(database);

// Create a new collection.
db.createCollection(customer_collections);
db.createCollection(table_collections);